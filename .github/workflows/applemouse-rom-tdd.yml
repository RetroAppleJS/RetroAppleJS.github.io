name: AppleMouse ROM TDD

on:
  push:
    branches:
      - feature/applemouse-authentic-rom

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch, test, and commit AppleMouse ROM integration
        run: bash tools/applemouse_rom_tdd.sh

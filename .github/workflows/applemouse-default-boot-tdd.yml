name: AppleMouse default boot TDD

on:
  push:
    branches:
      - feature/applemouse-authentic-rom

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch, test, and commit AppleMouse default boot integration
        run: bash tools/applemouse_default_boot_tdd.sh

name: AppleMouse default boot TDD

on:
  push:
    branches:
      - feature/applemouse-authentic-rom

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run focused AppleMouse boot suite
        run: node --test tests/applemouse_default_boot.test.js tests/applemouse_68705.test.js tests/applemouse_rom.test.js
